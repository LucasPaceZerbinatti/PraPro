BEGIN TRY

BEGIN TRAN;

-- CreateTable
CREATE TABLE [dbo].[Espec] (
    [id] INT NOT NULL,
    [nome] VARCHAR(50) NOT NULL,
    CONSTRAINT [Espec_pkey] PRIMARY KEY CLUSTERED ([id])
);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
